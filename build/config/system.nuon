{
  flatpak: {
    remotes: [
      {
        name: "flathub"
        url: "https://dl.flathub.org/repo/flathub.flatpakrepo"
      }
    ]
  }

  services: {
    disable: [
      "bootc-fetch-apply-updates.timer"
    ]
    enable: [
      "bluetooth.service"
      "ly@tty2.service"
      "firewalld.service"
      "avahi-daemon.service"
      "nix.mount"
      "nix-daemon"
      "fedora-nix-graphics-rebuild.service"
      "fedora-nix-gc.timer"
      "fedora-repair-passwd.service"
    ]
  }
}
