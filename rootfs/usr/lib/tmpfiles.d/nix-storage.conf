# Create /var/nix at boot. The /nix directory is preserved from the nix RPM
# install (bootc's / is read-only so it persists). nix-store-mount.service
# bind-mounts /var/nix over /nix so both paths share the same store.
d  /var/nix 0755 root root -
