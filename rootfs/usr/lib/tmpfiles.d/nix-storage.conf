# Create the Nix store directory in /var (writable, persistent across upgrades).
# /nix must NOT be a symlink (nix rejects symlinked store paths).
# Use a bind mount so /nix is an alias of /var/nix without being a symlink.
d  /var/nix 0755 root root -
b  /nix     -    -    -    - /var/nix
