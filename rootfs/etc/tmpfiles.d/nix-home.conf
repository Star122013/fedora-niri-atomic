# Bind mount /var/home to /home to satisfy Nix's requirement that
# /home is a real directory, not a symlink. This overrides the symlink
# /home -> /var/home created by bootc at build time.
# Data is still persisted in /var/home (the atomic /var layer).
L! /home - - - - -
b /home - - - - /var/home
